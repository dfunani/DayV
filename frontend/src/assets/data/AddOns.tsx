const AddOns: any = [
	["Balloon", "🎈", Math.ceil(Math.random() * 10)], 
	["Letter", "💌", Math.ceil(Math.random() * 10)],
	["Heart", "💘", Math.ceil(Math.random() * 10)],
	["Bouquet", "💐", Math.ceil(Math.random() * 50)],
	["Rose", "🌹", Math.ceil(Math.random() * 100)],
	["Ribbon", "🎀", Math.ceil(Math.random() * 100)],
	["Wrapped", "🎁", Math.ceil(Math.random() * 100)],
	["Teddy", "🧸", Math.ceil(Math.random() * 100)],
	["Ring", "💍", Math.ceil(Math.random() * 1000)]
]

export default AddOns
